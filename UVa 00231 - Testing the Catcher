#include <stdio.h>

int main() {
	int n, max = -1000000;
	int ans = findMaxHeight
}
int findMaxHeight(int cur, int num) {
	int n = 0;
	if (fscanf("%d", &n) == 1 && n > -1) {
		if (cur > n) {
			max = findMax(findMaxHeight(n, num+1), findMaxHeight(cur, num));
		}
	}
	return max;
}
